<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_radius_white_bg"
        android:orientation="vertical"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent="0.8">

        <TextView
            android:id="@+id/tv_setting_gallery"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:drawableLeft="@drawable/get_photo_from_gallery"
            android:drawablePadding="10dp"
            android:gravity="center_vertical|left"
            android:paddingLeft="15dp"
            android:text="@string/photo_gallery"
            android:textColor="@color/text_black_4d4d4d" />

        <View style="@style/divider_line" />

        <TextView
            android:id="@+id/tv_setting_camera"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:drawableLeft="@drawable/get_photo_from_camera"
            android:drawablePadding="10dp"
            android:gravity="center_vertical|left"
            android:paddingLeft="15dp"
            android:text="@string/photo_camera"
            android:textColor="@color/text_black_4d4d4d" />


    </LinearLayout>
</android.support.constraint.ConstraintLayout>